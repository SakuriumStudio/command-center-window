# browser emergency backup

更新日: 2026-03-16

## このファイルの役割

ここは保険枠の導線です。
本線は `GitHub共有窓 + ブラウザ運用` で、ここを主役にはしません。

## 使う条件

- ブラウザ運用だけでは足りない
- 回線条件が比較的良い
- 親機が起きている

## 今回の扱い

- `Sunshine / Moonlight` は保険枠
- `外部WOL` は今回は保留
- 病院中の本線はここではなくブラウザ側

## 切り替える時の考え方

1. まずブラウザ運用で足りるか確認する
2. 足りなければ `Sunshine / Moonlight` を検討する
3. 親機が寝ている場合は、今回は外部WOLに頼らない
4. 無理ならブラウザ本線に戻して status に必要事項を残す

## 覚えておくこと

- 親機側は `画面オフは可 / 本体スリープなし` が前提
- `povo` や回線条件が整った時だけ本格使用を考える
- ここは保険であって、今回の主ルートではない
